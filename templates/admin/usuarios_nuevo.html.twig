{% extends "base.html.twig" %}

{% block body %}
    <div class="row">
        <div class="col s12">
            <form class="card">
                <div class="card-content row noMargin">
                    <span class="card-title">Nuevo {{type}}</span>
                    {{form(form)}}
                </div>
            </form>
        </div>
    </div>
{% endblock %}